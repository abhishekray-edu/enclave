# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
.output
stats.html
stats-*.json
.wxt
web-ext.config.ts

# pocket-tts tokenizer test fixture — not redistributed (upstream model terms). Regenerate
# per lib/__tests__/fixtures/README.md; the tokenizer test auto-skips when it's absent.
lib/__tests__/fixtures/tokenizer.model

# Generated pocket-tts voice-model assets (~126 MB) — hosted on Hugging Face, never in git.
# Rebuild with `node scripts/prepare-tts-assets.mjs`.
tts-assets/

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
